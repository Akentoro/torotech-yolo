{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DetectRequest",
  "description": "POST /detect — multipart/form-data（所有參數為 Form field，非 Query param）",
  "type": "object",
  "properties": {
    "file": {
      "type": "string",
      "format": "binary",
      "description": "圖片檔案 (JPEG/PNG/BMP/TIFF)，最大 10MB"
    },
    "project": {
      "type": "string",
      "description": "專案 ID（預設 server_config 的 default_project）",
      "default": "robot_test_c4",
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9_-]{0,63}$"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0.5,
      "description": "信心度門檻"
    },
    "version": {
      "type": "string",
      "description": "模型版本（預設使用 active version），用於 A/B 切換",
      "pattern": "^v\\d+$",
      "default": null
    }
  },
  "required": ["file"],
  "_note": "BREAKING CHANGE v2: project/confidence/version 改為 Form field（非 Query param），C# 端用 MultipartFormDataContent.Add(new StringContent(...), 'project')"
}
